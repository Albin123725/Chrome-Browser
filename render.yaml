services:
  - type: web
    name: chrome-rdp
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    port: 3000  # Use Node.js port for Render health checks
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: VNC_PASSWORD
        value: "chrome123"
